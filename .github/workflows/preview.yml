name: Preview (disabled)

# Auto-deploy to kraskadoska.ru / other domains is disabled.
# Production deploys only via production.yml → krashenayadoska.ru on push to main.
on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy target notice
        run: |
          echo "Preview deploy is disabled."
          echo "Push to main to deploy https://krashenayadoska.ru via Production Deploy."
          exit 0
